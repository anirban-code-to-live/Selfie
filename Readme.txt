How Git Works:
	1. Initialise the folder using git init at the location of the directory you want to use as yoyr local git repo.
	2. Create a project in GitHub (preferably with same name as your local repository name, jut for your convenience)
	3. r