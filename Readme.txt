How Git Works:
	1. Initialise the folder using git init at the location of the directory you want to use as your local git repo.
	2. Create a project in GitHub (preferably with same name as your local repository name, just for your convenience)
	3. Push your project to GitHub using push